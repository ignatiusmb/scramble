function randnum(min,max){return Math.floor(Math.random()*(max-min+1))+min}function jumble(textLen){let newText="";for(let i=0;i<textLen;i++)newText+=String.fromCharCode(randnum(33,126));return newText}function scramble(textElement){const initialName=textElement.textContent,nameLen=textElement.textContent.length;function scramble(decodedLetters){let newText="";decodedLetters>0&&(newText=initialName.substr(0,decodedLetters)),textElement.textContent=newText+jumble(nameLen-decodedLetters)}let letterIndex=0,scrambleTimer=setInterval(function(){scramble(letterIndex)},30),letterIteration=setInterval(function(){clearInterval(scrambleTimer),scrambleTimer=setInterval(function(){scramble(letterIndex)},30),letterIndex++===nameLen&&(clearInterval(scrambleTimer),clearInterval(letterIteration))},300)}